# Automation Policy

Ask before commits, pushes, merges, pull requests, dependency installs, worktree changes, long-running commands, networked commands, destructive git actions, CI changes, deployments, or external automation.
