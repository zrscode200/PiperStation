# Architecture

`core/` stores shared behavior. `adapters/` stores harness mechanics. `generated/` stores rendered hub templates. `bootstrap/init.sh` composes selected runtimes into a target hub while preserving `projects/`.
