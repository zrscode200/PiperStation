{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "hook=.deepagents/hooks/session-context.sh; [ -f \"$hook\" ] && sh \"$hook\" || true",
            "timeout": 10,
            "statusMessage": "Loading Piper Station hub context"
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "matcher": "manual|auto",
        "hooks": [
          {
            "type": "command",
            "command": "printf %s '{\"systemMessage\": \"Piper Station compact reminder: durable state must already live in the project work records. If active work is in flight, ensure projects/<id>/work/context-pack.md was rewritten in full to the current boundary with only the non-derivable fields (goal, boundary and status, next exact action, verification and review state not yet in build-log.md, blockers and risks and open questions, stop reason, optional broad-search triggers or resume note) before relying on summarized context; branch, HEAD, status, and changed files are derived live at resume. This hook reminds; it does not block compaction.\"}'",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
